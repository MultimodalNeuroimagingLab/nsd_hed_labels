version: 2

mkdocs:
  configuration: mkdocs.yml

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  commands: 
    - pip install -r ./docs/requirements.txt 
    - python ./docs/write_viewer_mkfile.py
