version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
    install:
      - requirements: docs/requirements.txt

  commands: 
    - python ./docs/write_viewer_mkfile.py

mkdocs:
  configuration: mkdocs.yml