version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  commands: 
    - pip install -r ./docs/requirements.txt 
    - python ./docs/write_viewer_mkfile.py --output _readthedocs/html/ docs/

mkdocs:
  configuration: mkdocs.yml


python:
  install:
    - requirements: docs/requirements.txt
